{
  "name": "@skycoin4444/mobile-shell",
  "version": "1.0.0-beta.1",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "jest --runInBand",
    "audit:runtime": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-native": "^0.81.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/react": "^19.1.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.8.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.ts"]
  }
}
